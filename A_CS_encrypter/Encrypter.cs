using System;

//Encrypts a given text by changing each character to the following letter in the alphabet. Decrypts too.
namespace A_CS_encrypter {

    internal class Encrypter {

        static string Encrypt(string textToEncrypt) {
            var encryptedText = "";

            return encryptedText; }

        static string Decrypt(string textToDecrypt) {
            var decryptedText = "";

            return decryptedText; }

        static void Main(string[] args) {
            Console.WriteLine(Encrypt("Hello"));
            Console.WriteLine(Decrypt("Ifjjp")); }
    }
}
